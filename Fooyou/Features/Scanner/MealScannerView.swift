import SwiftUI

// Stub — implemented in Phase 3 (Claude Vision)
struct MealScannerView: View {
    var body: some View {
        ContentUnavailableView(
            "Binnenkort beschikbaar",
            systemImage: "camera.viewfinder",
            description: Text("Maaltijdfoto scannen met Claude Vision komt in Phase 3.")
        )
    }
}
