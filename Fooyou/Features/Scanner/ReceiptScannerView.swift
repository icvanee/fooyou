import SwiftUI

// Stub — implemented in Phase 1b (Share Extension + Claude)
struct ReceiptScannerView: View {
    var body: some View {
        ContentUnavailableView(
            "Binnenkort beschikbaar",
            systemImage: "doc.text.viewfinder",
            description: Text("Kassabon scannen komt in Phase 1b via de Share Extension.")
        )
    }
}
